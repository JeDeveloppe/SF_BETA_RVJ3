<form method="post" action="{{ path('app_boite_delete', {'id': boite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boite.id) }}">
    <button class="btn">Delete</button>
</form>
