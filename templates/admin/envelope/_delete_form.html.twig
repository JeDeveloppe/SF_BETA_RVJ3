<form method="post" action="{{ path('app_envelope_delete', {'id': envelope.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ envelope.id) }}">
    <button class="btn">Delete</button>
</form>
