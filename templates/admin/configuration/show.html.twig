{% extends 'admin/base.html.twig' %}

{% block title %}Gestion de la configuration du site{% endblock %}

{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>DevisDelayBeforeDelete</th>
                <td>{{ configuration.DevisDelayBeforeDelete }}</td>
            </tr>
            <tr>
                <th>PrefixeFacture</th>
                <td>{{ configuration.prefixeFacture }}</td>
            </tr>
            <tr>
                <th>PrefixeDevis</th>
                <td>{{ configuration.prefixeDevis }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_configuration_index') }}">back to list</a>

    <a href="{{ path('app_configuration_edit', {'id': configuration.id}) }}">edit</a>

    {{ include('admin/configuration/_delete_form.html.twig') }}
{% endblock %}
