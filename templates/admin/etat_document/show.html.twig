<!DOCTYPE html>

<title>EtatDocument</title>

{% block body %}
    <h1>EtatDocument</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etat_document.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ etat_document.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etat_document_index') }}">back to list</a>

    <a href="{{ path('app_etat_document_edit', {'id': etat_document.id}) }}">edit</a>

    {{ include('etat_document/_delete_form.html.twig') }}
{% endblock %}
