{% extends 'site/base.html.twig' %}

{% block title %}Devis non trouver{% endblock %}

{% dump(tableau) %}

{% block body %}

    <div class="row justify-content-center my-5">

        <div class="jumbotron col-11 col-md-6">
        <h1 class="display-4">{{ tableau['h1'] }}</h1>
        <p class="lead">{{ tableau['p1'] }}</p>
        <hr class="my-4">
        <p>{{ tableau['p2'] }}</p>
        <p class="lead">
            <a class="btn btn-primary btn-lg" href="{{ path('accueil') }}" role="button">Retour à l'accueil...</a>
        </p>
        </div>

    </div>


{% endblock %}
