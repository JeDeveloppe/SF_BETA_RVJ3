<!DOCTYPE html>

<title>Role</title>

{% block body %}
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ role.color }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ role.level }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_role_index') }}">back to list</a>

    <a href="{{ path('app_admin_role_edit', {'id': role.id}) }}">edit</a>

    {{ include('admin_role/_delete_form.html.twig') }}
{% endblock %}
