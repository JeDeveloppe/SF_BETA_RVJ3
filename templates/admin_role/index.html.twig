<!DOCTYPE html>

<title>Role index</title>

{% block body %}
    <h1>Role index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Color</th>
                <th>Level</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for role in roles %}
            <tr>
                <td>{{ role.id }}</td>
                <td>{{ role.color }}</td>
                <td>{{ role.level }}</td>
                <td>
                    <a href="{{ path('app_admin_role_show', {'id': role.id}) }}">show</a>
                    <a href="{{ path('app_admin_role_edit', {'id': role.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_role_new') }}">Create new</a>
{% endblock %}
